import React from "react";
import { Section } from "./shared.types";

interface SectionEventProps {
	courseTitle: string,
	sectionType: string,
	section: Section
}

export const SectionEvent = ({courseTitle, sectionType, section}: SectionEventProps) => {

}